version: '2'

services:
  lde.web:
    image: lde.web
    build:
      context: ./LDE.Web
    dockerfile: Dockerfile
   restart: always
   depends_on: 
     - "mongoDB"
  mongoDB:
    image: mongo
    volumes:
      - /var/mongo:/data/db
     ports:
      - 27017:27017
      restart: always